name: Price Scraper

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger price scrape
        run: |
          curl -X POST "${{ secrets.RENDER_APP_URL }}/api/prices/scrape-all"
